<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render 'profile' %>
        <%= render 'feed' %>
        <%= render 'who-to-follow' %>
      <% else %>
        <%= render 'trends' %>
        <%= render 'content' %>
      <% end %>

    </div>
  </div>
</section>
